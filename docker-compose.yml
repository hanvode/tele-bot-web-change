services:
  api-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-monitor
    restart: unless-stopped
    env_file:
      - ./.env
    volumes:
      - ./logs:/app/logs
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: redis
    ports:
      - "6379:6379"
    
  worker:
    build: .
    container_name: monitor-worker
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    command: ["node", "dist/queues/monitorWorker.js"] 